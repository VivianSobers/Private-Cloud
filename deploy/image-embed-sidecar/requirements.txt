# Pinned loosely on purpose, the same way the other three sidecars are: this is
# an inference sidecar the operator builds for their own hardware, and torch's
# right wheel depends on whether that is CPU or a specific CUDA version.
# transformers pulls torch in; install a matching torch wheel first if you want a
# particular CUDA build rather than the default.
fastapi>=0.110
uvicorn[standard]>=0.29
transformers>=4.40
torch>=2.2
pillow>=10.3
numpy>=1.24
