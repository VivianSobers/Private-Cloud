# NUT mode — /etc/nut/nut.conf
#
# `standalone`: one machine, one UPS, driver + server + monitor all local. The
# alternative (`netserver`) is for a UPS shared with other machines, which is a
# larger surface than a single-server design needs.
MODE=standalone
