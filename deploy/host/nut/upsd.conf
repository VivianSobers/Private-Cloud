# NUT server configuration — /etc/nut/upsd.conf
#
# Listen on loopback only. upsd exists to tell one process on this machine that
# the power has failed; exposing it on the tailnet would add a network surface
# whose only purpose would be to let another machine shut this one down.
LISTEN 127.0.0.1 3493

MAXAGE 15
